{
  "permissions": {
    "allow": [
      "Skill(run)",
      "Skill(run:*)",
      "Bash(timeout 120 bash -c 'until curl -sf http://localhost:3000 >/dev/null; do sleep 2; done')",
      "Bash(chromium-cli --session portfolio)",
      "Bash(timeout 120 bash -c 'until grep -qE \"Compiled|Failed to compile|error\" /tmp/dev.log 2>/dev/null; do sleep 2; done')",
      "Read(//tmp/**)",
      "Bash(timeout 180 bash -c 'until grep -qE \"Compiled|Failed to compile\" /tmp/dev.log 2>/dev/null; do sleep 3; done')",
      "Bash(timeout 180 bash -c 'until grep -qE \"Compiled|Failed to compile|webpack compiled\" /tmp/dev2.log 2>/dev/null; do sleep 3; done')",
      "Bash(curl -s http://localhost:3001/)"
    ]
  }
}
